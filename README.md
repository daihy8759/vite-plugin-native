# vite-plugin-native

## install

```
npm i vite-plugin-native -D
```

## Usage

vite.config.js:

```javascript
import native from 'vite-plugin-native';

const config = {
    plugins: [native()],
};
```
